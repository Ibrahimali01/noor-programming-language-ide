# مكتبة قواعد البيانات غير الهيكلية (Noor NoSQL)
اكتب("📦 تم تحميل مكتبة مستودعات البيانات السريعة (std_no_sql).")

دالة حفظ_مستند(مجموعة, بيانات) {
  اكتب("💾 تم حفظ المستند في مجموعة:", مجموعة)
  ارجع صحيح
}
