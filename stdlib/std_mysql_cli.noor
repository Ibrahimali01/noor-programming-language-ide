# قواعد البيانات (std_mysql_cli)
اكتب("🐬 تم تحميل عميل MySQL.")
دالة استعلام_ماي_سيكوال(قاعدة, يوزر, باس, استعلام) { ارجع تنفيذ_نظام("تنفيذ_أمر", "mysql -u " + يوزر + " -p" + باس + " -D " + قاعدة + " -e "" + استعلام + """) }
