# مستعلم البيانات (std_graphql)
اكتب("🕸️ تم تحميل عميل GraphQL.")
دالة استعلام_جراف(رابط, استعلام) { ارجع تنفيذ_نظام("تنفيذ_أمر", "curl -s -X POST -H "Content-Type: application/json" -d "{\"query\": \"" + استعلام + "\"}" "" + رابط + """) }
